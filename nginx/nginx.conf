events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # HTTP server - redirect to HTTPS (after SSL setup)
    server {
        listen 80;
        server_name aiclub-bitp www.aiclub-bitp;

        # Certbot challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # Redirect all other traffic to HTTPS (comment out initially)
        # location / {
        #     return 301 https://$server_name$request_uri;
        # }

        # TEMPORARY: Serve site on HTTP until SSL is configured
        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
        }

        location /api/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /admin/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
        }

        location /static/ {
            alias /app/staticfiles/;
        }

        location /media/ {
            alias /app/media/;
        }
    }

    # HTTPS server (uncomment after SSL is obtained)
    # server {
    #     listen 443 ssl http2;
    #     server_name aiclub-bitp www.aiclub-bitp;
    #
    #     ssl_certificate /etc/letsencrypt/live/aiclub-bitp/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/aiclub-bitp/privkey.pem;
    #
    #     # Frontend
    #     location / {
    #         root /usr/share/nginx/html;
    #         try_files $uri $uri/ /index.html;
    #     }
    #
    #     # Backend API
    #     location /api/ {
    #         proxy_pass http://backend:8000;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #
    #     # Admin
    #     location /admin/ {
    #         proxy_pass http://backend:8000;
    #         proxy_set_header Host $host;
    #     }
    #
    #     # Static files
    #     location /static/ {
    #         alias /app/staticfiles/;
    #     }
    #
    #     # Media files
    #     location /media/ {
    #         alias /app/media/;
    #     }
    # }
}