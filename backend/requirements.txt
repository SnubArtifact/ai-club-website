# Use stable Django 5.1 instead of dev version
Django==5.1.3
djangorestframework==3.15.2
django-cors-headers==4.6.0
Pillow==11.0.0
python-dotenv==1.0.1

# PostgreSQL adapter (production)
psycopg2-binary==2.9.10

# Production server
gunicorn==23.0.0

# Optional: For better production performance
whitenoise==6.8.2